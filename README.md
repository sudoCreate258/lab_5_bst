# lab_5_bst

```
Implement a binary search tree that will have the following:
Be able to add data
Delete data
Search for data
Print out the binary tree in BFS mode (see slide 58)
Submit all code including several test case output.
All code must be done by you. You cannot use the build in binary tree that is part of java SDK.
```
